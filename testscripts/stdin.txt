# Test reading from stdin when no args are given.

stdin input.tpl.go
gotmplfumpt
cmp stdout expected.tpl.go

# Test that -w with stdin fails.
! gotmplfumpt -w
stderr 'gotmplfumpt: cannot use -w with standard input'

-- input.tpl.go --
package main

func F() {
return
}
-- expected.tpl.go --
package main

func F() {
	return
}
