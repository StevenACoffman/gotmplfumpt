# Test -l flag lists files whose formatting differs.

gotmplfumpt -l unformatted.tpl.go formatted.tpl.go
stdout '^unformatted.tpl.go$'
! stdout '^formatted.tpl.go$'

# Source files should be unchanged.
cmp unformatted.tpl.go unformatted.orig.tpl.go

# Test that -l with stdin fails.
! gotmplfumpt -l
stderr 'gotmplfumpt: cannot use -l with standard input'

-- unformatted.tpl.go --
package main

func F() {
return
}
-- unformatted.orig.tpl.go --
package main

func F() {
return
}
-- formatted.tpl.go --
package main

func F() {
	return
}
